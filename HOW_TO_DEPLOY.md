---
description: Vercel을 이용한 무료 배포 가이드
---

# 🚀 누구나 접속 가능한 '진짜 웹사이트' 만들기 (배포)

지금은 컴퓨터 안에서만 돌아가지만, 친구들이 LTE로도 접속하게 하려면 **Vercel(버셀)**이라는 무료 서비스에 올리면 됩니다.

## 1단계: 코드를 GitHub(깃허브)에 올리기
가장 먼저 작성한 코드를 GitHub라는 코드 저장소에 올려야 합니다.

1. **GitHub 계정 만들기**: [GitHub.com](https://github.com)에 가서 회원가입을 하세요.
2. **새 저장소(Repository) 만들기**:
   - 우측 상단 `+` 버튼 -> `New repository` 클릭
   - Repository name에 `anonymous-list` 입력
   - `Public` 또는 `Private` 선택 (Private 추천)
   - `Create repository` 버튼 클릭
3. **코드 업로드**:
   - 터미널에 아래 명령어를 한 줄씩 입력하세요 (GitHub 화면에 나오는 명령어 복사):
   ```bash
   git remote add origin https://github.com/사용자아이디/anonymous-list.git
   git branch -M main
   git push -u origin main
   ```

## 2단계: Vercel에 연결하기
1. **[Vercel.com](https://vercel.com)** 접속 후 회원가입 (GitHub 아이디로 로그인하면 편합니다).
2. 대시보드에서 `Add New...` -> `Project` 클릭.
3. 방금 만든 `anonymous-list` 저장소 옆의 `Import` 버튼 클릭.

## 3단계: 환경 변수 설정 (중요!)
배포 화면 아래쪽에 **Environment Variables**라는 섹션이 있습니다. 여기에 Supabase 연결 정보를 넣어줘야 합니다.

| Key (이름) | Value (값) |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | `.env.local` 파일에 있는 URL 값 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `.env.local` 파일에 있는 KEY 값 |

- `.env.local` 파일 내용을 복사해서 각각 붙여넣으세요.

## 4단계: 배포 시작
- `Deploy` 버튼을 누르면 끝입니다!
- 1~2분 정도 기다리면 폭죽이 터지며 **`https://anonymous-list-xxxx.vercel.app`** 같은 진짜 주소가 나옵니다.

## 5단계: Supabase 설정 업데이트 (마지막)
배포된 주소를 Supabase가 알게 해줘야 로그인이 됩니다.

1. **Supabase 대시보드** 접속 -> `Authentication` -> `URL Configuration`.
2. **Site URL**에 방금 생긴 Vercel 주소(`https://...`)를 입력.
3. **Redirect URLs**에도 Vercel 주소 뒤에 `/auth/callback`을 붙여서 추가.
   - 예: `https://anonymous-list-xxxx.vercel.app/auth/callback`
4. 저장(Save).

🎉 **이제 친구들에게 URL을 카톡으로 보내면 LTE로 접속할 수 있습니다!**
